/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'avatars.githubusercontent.com', 'www.buymeacoffee.com'],
  }
}

module.exports = nextConfig
